---
language: go

sudo: false

go:
  - 1.7

services:
  - rabbitmq

branches:
  only:
    - master

before_install:
  - go get github.com/mattn/goveralls

script:
  - goveralls -v -service=travis-ci -ignore="examples/worker/worker.go,examples/sender/sender.go"
